<% if @boilers.any? %>
  <% @boilers.each do |boiler| %>
    <div><%= boiler.name %></div>
  <% end %>
<% else %>
  <h1>No boilers in database</h1>
<% end %>

<% content_for :header do%>
	<h1><%= @category.nil? ? 'All Boilers' : @category.name %></h1>
<% end %>